post:
  summary: Creates project
  responses:
    "201":
      description: Creates project under a user id
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../components/schemas/project.yaml"