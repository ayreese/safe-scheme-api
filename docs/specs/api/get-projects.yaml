get:
  summary: Get a list of projects for a user
  responses:
    "200":
      description: A list of projects
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../components/schemas/project.yaml'